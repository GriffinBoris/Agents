# Objective

Perform the bounded second correction pass using the final guidance and acceptance reviews.

Resolve every remaining actionable finding and add any missing test evidence. If both reviews pass with no findings, make no source changes and report that explicitly. Do not weaken tests or guidance compliance to obtain a pass. Preserve unrelated changes and do not commit or push.

If a blocker cannot be resolved without user direction or meaningful scope expansion, ask the Desktop parent and wait instead of guessing.

# Output

Return a concise Markdown ledger of final findings, dispositions, changes, and focused validation.
